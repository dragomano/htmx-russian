---
title: Атрибут hx-patch
description: Описание атрибута hx-patch в htmx
origin: https://htmx.org/attributes/hx-patch/
sidebar:
  label: hx-patch
---

Атрибут `hx-patch` заставит элемент отправить `PATCH`-запрос по указанному URL и заменить HTML в DOM, используя стратегию замены:

```html wrap
<button hx-patch="/account" hx-target="body">
  Обновить учётку
</button>
```

Этот пример заставит `button` отправить `PATCH`-запрос на `/account` и заменить возвращаемый HTML в `innerHTML` элемента `body`.

## Примечания

- `hx-patch` не наследуется.
- Вы можете контролировать цель замены с помощью атрибута [hx-target](../hx-target).
- Вы можете контролировать стратегию замены, используя атрибут [hx-swap](../hx-swap).
- Вы можете контролировать, какое событие вызывает запрос, с помощью атрибута [hx-trigger](../hx-trigger).
- Вы можете контролировать данные, отправляемые с запросом, различными способами, описанными здесь: [Параметры](/ajax/parameters).
