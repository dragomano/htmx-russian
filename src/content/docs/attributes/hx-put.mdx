---
title: Атрибут hx-put
description: Описание атрибута hx-put в htmx
origin: https://htmx.org/attributes/hx-put/
sidebar:
  label: hx-put
---

Атрибут `hx-put` заставит элемент отправить `PUT`-запрос по указанному URL и заменить HTML в DOM, используя стратегию замены:

```html wrap
<button hx-put="/account" hx-target="body">
  Положить деньги на счёт
</button>
```

Этот пример заставит `button` отправить `PUT`-запрос на `/account` и заменить возвращаемый HTML в `innerHTML` элемента `body`.

## Примечания

- `hx-put` не наследуется.
- Вы можете контролировать цель замены с помощью атрибута [hx-target](../hx-target).
- Вы можете контролировать стратегию замены, используя атрибут [hx-swap](../hx-swap).
- Вы можете контролировать, какое событие вызывает запрос, с помощью атрибута [hx-trigger](../hx-trigger).
- Вы можете контролировать данные, отправляемые с запросом, различными способами, описанными здесь: [Параметры](/ajax/parameters).
